if (place_meeting(x, y, Player)) {
    if (Input_Pressed(Input.interact) && msg != "") {
        /*
        with (instance_create(0, 0, DialogBox)) {
            msg = other.msg;
        }
        */
    }
}
